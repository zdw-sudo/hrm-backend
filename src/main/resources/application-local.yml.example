# 复制本文件为 application-local.yml（该文件已被 .gitignore 忽略）
# IDEA：Active profiles 填 local（你当前已配置）
# 注意：必须复制为 application-local.yml，改 .example 文件不会生效！

spring:
  ai:
    openai:
      api-key: 在此填写你的 DeepSeek API Key
